# Gleam

Open up a Nix shell:

```shell
nix develop
```

Run `gleam` inside the shell:

```shell
gleam run
```

Alternatively, use Gleam as an app:

```shell
alias gleam='nix run . --'

gleam run
```
